'use strict'

const { createApp } = Vue

    createApp({
        data(){
            return {
             titoloBig: 'Questo è il messaggio'
            }
        }
    }).mount('#titolo');

    const titoloBig = 'Messaggione Titolone';